# الهدف: إعلانُ تبعيّاتِ جذرِ المستودعِ رسميًّا — ما تلزمُه بوّاباتُ CI واختباراتُ
#        الجذرِ (`tests/`)، في موضعٍ واحدٍ يُركَّبُ بأمرٍ واحد.
# النطاق: جذرُ المستودعِ وحدَه. تبعيّاتُ حزمةِ الخدماتِ مُعلَنةٌ في
#        `federal/executive/services/pyproject.toml` و`requirements.lock`، ولا
#        تُكرَّرُ هنا (القاعدة 6: لا مفهومَ ثانيًا موازيًا).
# المالك: governance/
# تاريخ الإنشاء: 2026-08-21
# تاريخ آخر تعديل: 2026-08-21
#
# ── لماذا هذا الملفّ (H-1N-2 · T0.1) ────────────────────────────────────────
# كانت تبعيّاتُ الجذرِ مبثوثةً في سطورِ `pip install` داخلَ ستِّ وظائفَ في
# `.github/workflows/ci.yml`، فلا يعرفُ الداخلُ الجديدُ ما يُركِّبُ إلّا بقراءةِ
# مِلفِّ CI سطرًا سطرًا، وكلُّ إضافةٍ تُنسى في وظيفةٍ فتسقطُ بوّابةٌ لسببٍ لا علاقةَ
# له بما تحرسُه. فالإعلانُ صار واحدًا، والوظائفُ تقرأُ منه، وحارسٌ يمنعُ العودةَ
# إلى البثِّ: `tests/governance/test_root_dependencies_declared.py`.
#
# الحدُّ الأدنى في كلِّ سطرٍ هو **المُقاسُ** في بيئةِ العملِ لا المُتمنّى، ولا
# يُدَّعى دعمُ أقدمَ منه لأنَّه لم يُقَس. وCI يعملُ على Python 3.12 والقياسُ هنا
# على 3.14 — وهو حدٌّ مُعلَنٌ (C-7) لا يُخفى.

# مُشغِّلُ الاختبارات — كلُّ بوّاباتِ `tests/` تستدعيه.
pytest>=8.3.3

# بوّاباتُ التغطيةِ الفرعيّةِ (الدستور ≥90٪ · السيادة ≥90٪ · التاج ≥90٪).
pytest-cov>=5.0.0

# Ed25519: `core.sovereignty.crown` وما يستوردُها — ومنها قواعدُ المادةِ العاشرة
# ومعجمُ المالِ الدستوريّ في `tests/governance/`. بغيابِها تسقطُ البوّاباتُ في
# الجمعِ بـ`ModuleNotFoundError` قبلَ أن يُفحَصَ حارسٌ واحد، ولا يُستعملُ
# `importorskip` دواءً: حارسٌ يُتخطَّى بصمتٍ ليس حارسًا (القاعدة 12).
cryptography>=43.0.1
