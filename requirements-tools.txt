# الهدف: إعلانُ تبعيّاتِ أدواتِ الجذرِ التي **لا** تستدعيها بوّاباتُ CI — تُركَّبُ
#        عند تشغيلِ تلك الأدواتِ وحدَها، فلا تُثقَلُ بها كلُّ وظيفة.
# النطاق: مولِّداتُ الجذرِ وأدواتُ الترحيلِ المذكورةُ أدناه بأسمائِها.
# المالك: governance/
# تاريخ الإنشاء: 2026-08-21
# تاريخ آخر تعديل: 2026-08-21
#
# ── لماذا مُنفصلٌ عن `requirements-dev.txt` (T0.7) ──────────────────────────
# التبعيّتان أدناه مُستخدَمتان في الكودِ فعلًا وغيرُ مُعلَنتَين في أيِّ مِلفٍّ قبلَ
# هذا — وهو ما رُصِدَ في H-1N-2. لكنَّهما لا تلزمان بوّابةً واحدةً في CI: الأداتان
# مولِّدتان تُشغَّلان بيدٍ عند الحاجة. فخَلْطُهما بتبعيّاتِ البوّاباتِ يُركِّبُهما في
# ستِّ وظائفَ بلا سببٍ مقيس، وحَجْبُهما إخفاءُ دَينٍ قائم. فالإعلانُ هنا: صريحٌ
# ومنفصلٌ ومربوطٌ بمُستخدِمِه.

# `agents/registry/generate_imported.py` — مولِّدُ هويّاتِ الوكلاءِ المستوردين.
PyYAML>=6.0.3

# `tools/migrations/r4_unify_agent_identity.py` — ترحيلُ R4 (يقرأُ قاعدةَ البيانات
# عبرَ `sqlalchemy.inspect` و`text`). والحدُّ الأدنى هو نفسُه المُعلَنُ في حزمةِ
# الخدماتِ حتى لا يتعارضَ الإعلانان في بيئةٍ واحدة.
SQLAlchemy>=2.0.35
