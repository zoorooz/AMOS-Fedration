# SOVEREIGN_MIGRATION_PROGRAM.md — برنامجُ الهجرةِ السياديّة

<!--
الهدف: إدارةُ برنامجِ نقلِ الكتاباتِ الإنتاجيّةِ إلى حدِّ التنفيذِ السياديِّ بعد 2A
النطاق: خطُّ الأساس · خريطةُ المراحل · قواعدُ التنفيذِ والالتزام · سجلُّ المراحلِ والأرقام
المالك: docs/audit/ — ديوانُ التدقيق
تاريخ الإنشاء: 2026-08-18
تاريخ آخر تعديل: 2026-08-18
tags: sovereign-migration, program-control, migration-debt, baseline
-->

## 1. التعريف والحدّ

هذا الملفُّ **سجلُّ إدارةِ برنامج**، لا وثيقةُ تصميمٍ ولا مصدرُ حقيقةٍ دستوريّ.
- النصُّ الدستوريُّ الحاكمُ في `core/constitution/`.
- القراراتُ المؤسِّسةُ في `docs/MASTER_PLAN.md`.
- تعريفُ «تمّ» في `docs/audit/DEFINITION_OF_DONE.md`.
- القراراتُ المعلَّقةُ في `docs/audit/SOVEREIGN_DECISION_REGISTER.md`.
- جردُ الدَّينِ في `docs/audit/MIGRATION_DEBT_INVENTORY.md`.

**إذا خالفَ هذا الملفُّ نصًّا دستوريًّا فالنصُّ هو الحاكم، وهذا الملفُّ يُصحَّح.**

---

## 2. خطُّ الأساس — مقيسٌ لا مُدَّعى

| البند | القيمة المقيسة |
|---|---|
| الفرع | `main` |
| `origin/main` | `6a86205ce635a777673dce7a125f6b6bfeec433e` |
| `HEAD` المحلّيُّ قبل المواءمة | `024f8ab149d4477321690bdd9fa5f8785e9ede83` |
| محتوى شجرةِ العملِ قبل المواءمة | **مطابقٌ حرفيًّا** لمحتوى `6a86205` (تعديلاتُ 2A كانت حاضرةً كتغييراتِ شجرةِ عمل) |
| إجراءُ المواءمة | `git reset --mixed 6a86205…` — يحرّكُ `HEAD` والفهرسَ **ولا يمسُّ ملفًّا** |
| ما لم يُستعمَل | `reset --hard` · `rebase` · `clean -fd` — لم يُنفَّذ أيٌّ منها، ولم يُفقَد تعديلٌ واحد |
| `HEAD` بعد المواءمة | `6a86205…` · شجرةُ العملِ **نظيفة** |
| اختباراتُ خطِّ الأساس | `test_2a_sovereign_runtime_integration` · `test_phase12_states` · `test_treasury` · `test_1n_boundary_integration` = **72 passed**؛ و`test_r7_state_registry` · `test_r7_government_services` · `test_r8_federal_state_integration` = **91 passed** |

### 2.1 القياسُ العدديُّ لخطِّ الأساس

الأداةُ: `tools/audit/sovereign_write_inventory.py` (أُضيفت في P0 لتكونَ الأرقامُ **قابلةً لإعادةِ التوليد**، لا مُقتبَسةً من تقريرٍ سابق).

```
python tools/audit/sovereign_write_inventory.py
```

| المقياس | خطُّ الأساس (6a86205) |
|---|---|
| مواضعُ كتابةٍ في المصدرِ الإنتاجيِّ (شاملةَ المعاونات) | **216** |
| عمليّاتٌ عامّةٌ مُغيِّرةٌ للحالة | **189** |
| منها تعبرُ الحدَّ السياديَّ | **2** |
| مساراتٌ قديمةٌ **مُغلَقة** (`UndeclaredExecutionError`) | **4** |
| عمليّاتٌ عامّةٌ **لا تعبرُ الحدَّ** | **185** |

| المنطقة | عامّة | سياديّة | متجاوزة |
|---|---|---|---|
| `services/` | 154 | 2 | 151 |
| `common/` (خدمات) | 14 | 0 | 14 |
| `core/` | 8 | 0 | 7 |
| `tools/` | 11 | 0 | 11 |
| `federal/` أخرى · أخرى | 2 | 0 | 2 |

### 2.2 مطابقةُ الأرقامِ مع تقريرِ الجردِ السابق — الفرقُ مُعلَنٌ لا مُخفى

تقريرُ الجردِ السابقُ ذكرَ «413 دالّةً ذاتَ أثرٍ كتابيّ» و«112 عمليّةً متجاوزة»، وهذا القياسُ يذكرُ 216 و185. الفرقُ **منهجيٌّ لا تناقضٌ**، وبيانُه:

| الفرق | السبب |
|---|---|
| 413 ← 216 | القياسُ السابقُ عدَّ نداءاتِ التدقيقِ (`audit.append`) ونشرِ الأحداثِ (`publish`) والدوالَّ الخاصّةَ المتداخلةَ **كتاباتٍ**. وهذه الأداةُ تحصرُ الكتابةَ في أثرِ قاعدةِ البيانات (`add/add_all/commit/delete/merge` أو SQL خامٍّ تغييريّ). |
| 112 ← 185 | الرقمُ 112 كان **نطاقًا منتقًى**: عائلاتُ الخدماتِ ذاتُ الصلةِ السياديّةِ وحدَها، وأُخرِج منه `common/` و`core/` و`tools/` ووحداتٌ مثلُ `health` و`learning_cycle` و`security`. والرقمُ 185 هو **الشجرةُ كاملةً بلا انتقاء**، وهو الرقمُ الذي يُتابَعُ في هذا البرنامج. |
| المتجاوزُ في `services/` | **151** — وهو الأقربُ إلى الرقمِ المنتقى 112 بعدَ إضافةِ الوحداتِ التي أُخرِجت منه. |

**الرقمُ المرجعيُّ للبرنامجِ من الآن:** `non_sovereign_write_operations` كما تُخرِجُه الأداةُ = **185** عند خطِّ الأساس.

### 2.4 تصحيحُ أداةِ القياسِ (v2) — عيبٌ اكتُشِفَ في P1أ فأُبطِلَ الرقمُ 185

أوّلُ ما أنتجته المرحلةُ P1أ لم يكن هجرةً بل **اكتشافَ خللٍ في المقياسِ نفسِه**، وتوثيقُه أولى من إخفائِه:

بعدَ هجرةِ `set_institution_status` نُقِلت كتابةُ الأثرِ إلى معاونٍ خاصٍّ (`_set_institution_status_row`) يُنادى **من داخلِ** `guard_declared` — وهذا هو النمطُ الصحيحُ لأنّ المعاونَ يصلحُ مُطبِّقًا للأثرِ ومُعوِّضًا له في وقتٍ واحد. فكانت النتيجةُ في المقياسِ v1:

| المقياس | قبلَ P1أ | بعدَ P1أ (v1) |
|---|---|---|
| عمليّاتٌ عامّةٌ مُغيِّرة | 189 | **188** |
| منها تعبرُ الحدَّ | 2 | **2** |
| لا تعبرُ الحدَّ | 185 | **184** |

أي أنّ العمليّةَ المُهاجَرةَ **اختفت** من العدِّ بدلًا من أن تنتقلَ إلى خانةِ «سياديّة». وهذا عيبٌ جوهريّ: مقياسٌ يُخفي التقدُّمَ يُخفي التقصيرَ كذلك، ولو مضى البرنامجُ عليه لصار انخفاضُ الرقمِ دليلًا غامضًا لا يُفرَّقُ فيه بين هجرةٍ حقيقيّةٍ ونقلِ كتابةٍ إلى معاونٍ بلا حدٍّ سياديّ أصلًا.

**الإصلاح** (`tools/audit/sovereign_write_inventory.py` v2): تُتَبَّعُ استدعاءاتُ المعاونينِ الخاصّينِ داخلَ الوحدةِ تتبُّعًا ساكنًا (بعمقٍ غيرِ محدودٍ وبحمايةٍ من الدوران)، فتُنسَبُ كتابةُ المعاونِ إلى العمليّةِ العامّةِ التي تُناديه، ولا يُعَدُّ المعاونُ عمليّةً ثانية. و«تعبرُ الحدَّ» تبقى مشروطةً بوقوعِ `guard_declared` في جسدِ العمليّةِ العامّةِ نفسِها — فالإعلانُ مسؤوليّةُ العمليّةِ لا مسؤوليّةُ أداةِ قياس. ومعاونٌ يكتبُ ولا تصلُ إليه عمليّةٌ عامّةٌ لا يُسقَطُ بل يُحصى موضعًا مستقلًّا.

**وأُعيدَ قياسُ خطِّ الأساسِ نفسِه (6a86205) بالأداةِ v2** حتى تبقى الأرقامُ قابلةً للمقارنة — ولم يُنقَلْ رقمٌ قديمٌ إلى مقياسٍ جديد:

| المقياس | خطُّ الأساس (6a86205) · v1 | خطُّ الأساس (6a86205) · **v2** |
|---|---|---|
| مواضعُ كتابةٍ | 216 | **225** |
| عمليّاتٌ عامّةٌ مُغيِّرة | 189 | **213** |
| منها تعبرُ الحدَّ | 2 | **3** |
| مساراتٌ قديمةٌ مُغلَقة | 4 | **4** |
| لا تعبرُ الحدَّ | 185 | **208** |

سببُ الارتفاعِ: v1 كان يُسقِطُ كلَّ عمليّةٍ عامّةٍ تكتبُ **بمعاونِها** لا بجسدِها — وهي 24 عمليّةً قائمةً قبلَ هذا البرنامجِ بأكملِه، لا أحدثَتها هجرةٌ. أي أنّ دَينَ الهجرةِ الحقيقيَّ كان **أكبرَ** من المُعلَن، ورقمُ 185 كان مُطمئِنًا بغيرِ حقّ. وعددُ العابراتِ للحدِّ 3 لا 2، والفرقُ عمليّةٌ عامّةٌ تعبرُ الحدَّ وكتابتُها في معاونِها لم تكن تُرى.

**الرقمُ المرجعيُّ للبرنامجِ من الآن: 208 عند خطِّ الأساس** (v2). وكلُّ رقمٍ سابقٍ في هذه الوثيقةِ قيسَ بـ v1 ويُقرأُ في سياقِه لا يُقارَنُ به.

---

### 2.3 حالةُ 2A

| البند | الحالة |
|---|---|
| `state_registry.register_institution` | **PROVEN** — عبرَ `guard_declared`، ومسارُها القديمُ مُغلَق |
| `governance.state_runtime.allocate_budget` | **PROVEN** — بفاعلِ `TREASURY`، ومسارُها القديمُ مُغلَق |
| الصياغةُ المُعتمَدة | «تم إثبات نمط Sovereign Runtime Integration على كتابتين إنتاجيتين مختلفتين، وتم إغلاق مساراتهما القديمة، مع بقاء بقية نقاط التجاوز القائمة كـ migration debt.» |
| صياغةٌ **محرَّمة** | «State Runtime أصبح Sovereign بالكامل» |

---

### 2.5 تصحيحٌ ثانٍ في أداةِ القياس (v3) — تضخيمٌ لا نقص

عندَ إغلاقِ المسارِ القديمِ المنسوبِ إلى `state_registry/main.py` تبيّنَ أنَّ **لا كتابةَ
فيه أصلًا**: ما عدّته الأداةُ كتابةً هو مُزخرِفُ مسارٍ اسمُه `@router.delete(...)`.
والفحصُ الشاملُ أظهرَ أنَّ الأداةَ كانت تعدُّ كلَّ `add/delete/commit` كتابةً في قاعدةٍ
أيًّا كانَ مُستقبِلُها — فـ`seen.add(...)` و`report.add(...)` و`self._by_role[...].add(...)`
كلُّها كانت «كتاباتٍ سياديّةً متجاوزة».

**v3** تشترطُ أن يكونَ المُستقبِلُ جلسةَ تخزينٍ (جلسة · اتّصال · مُؤشِّر · محرّك ·
مستودع). وأُعيدَ القياسُ على نقطةِ الأساسِ `6a86205` وعلى الرأسِ بالأداةِ نفسِها:

| القياس | نقطةُ الأساس | بعدَ P1 (أ·ب·ج·د) |
|---|---|---|
| مواضعُ كتابة | 197 | 199 |
| عمليّاتٌ عامّةٌ مُغيِّرة | 186 | 186 |
| تعبرُ الحدّ | 3 | **7** |
| مساراتٌ قديمةٌ مُغلَقة | 4 | 4 |
| **لا تعبرُ الحدّ** | **181** | **177** |

فالرقمُ المرجعيُّ صارَ **181** بعدَ أن كانَ 208 في v2 و185 في v1. والفرقُ ليس تحسُّنًا:
هو أنَّ 27 موضعًا لم تكنْ كتاباتٍ قطّ. وأرقامُ تقريرَي P1أ و P1ب (208→207→206) صحيحةٌ
**في مقياسِها**، وقد أُعيدَ تأسيسُها هنا: 181 → 177 بعبورِ أربعِ عمليّات.

**وهذا ثاني عيبٍ في أداةِ القياسِ يُكتشَفُ في هذا البرنامج.** الأوّلُ كانَ نقصًا (هجرةٌ
صحيحةٌ تُخفي العمليّةَ من العدّ)، والثاني تضخيمًا. والنتيجةُ المُعلَنة: **رقمُ الدَّينِ
تقديرٌ بأداةٍ ساكنةٍ لا حصرٌ مُحقَّق**، وحدودُها مكتوبةٌ في رأسِ مخرَجِها. ولا يُبنى على
تحرُّكِه وحدَه حكمٌ بإنجاز؛ الحكمُ على العمليّةِ يُبنى على فحوصِها.

**نتيجةٌ ثانيةٌ لازمة:** بندُ P1 «إغلاقُ المسارِ القديمِ في `state_registry/main.py`»
**يسقُط** — لا مسارَ هناك يُغلَق. و`main.py` طبقةُ مسالكَ تُفوِّضُ إلى الخدمةِ ولا تكتبُ
في قاعدةٍ. وقد قيسَ ذلك بشجرةِ المصدرِ لا بالعينِ: لا دالّةَ في الوحدةِ يقعُ في جسمِها
نداءُ كتابةٍ على جلسة.

**وحدةُ `state_registry` تمامًا:** 5 عمليّاتٍ عامّةٍ مُغيِّرةٍ، **كلُّها** تعبرُ الحدَّ،
ولا واحدةَ متجاوزةً — وهذا حدُّ ما تشهدُ به الأداةُ عن هذه الوحدةِ وحدَها، لا عن
النظام.

## 3. الهدفُ العامّ

نقلُ المستودعِ تدريجيًّا من «نمطٌ مُثبَتٌ على عمليّتين» إلى «الحدُّ السياديُّ هو المسارُ الإنتاجيُّ القياسيُّ لكلِّ كتابةٍ يثبتُ اختصاصُها»، **بلا** اختراعِ بدائيّةٍ سياديّةٍ جديدةٍ ولا مصدرِ حقيقةٍ ثانٍ ولا كسرِ عقودِ 1F–1N، ومع إدارةٍ منهجيّةٍ لما لا يجوزُ نقلُه بلا قرارٍ بشريّ.

**سندُ الهجرةِ نفسِها** ليس قرارَ مُنفِّذ: `docs/MASTER_PLAN.md § 4` البندُ السادسُ يحسمُ نطاقَ 1N بأنّه «هجرةُ المُنادين إلى الحدِّ بحفظِ سلوكِهم العامّ»، **وبه أُغلِقَ القرارُ البشريُّ #8: «هل تُهاجَرُ المواضعُ القائمةُ إلى الحدّ؟ — نعم، تُهاجَر»**. فالبرنامجُ تنفيذٌ لقرارٍ قائمٍ لا استحداثُ قرار. وما خرجَ عن ذلك — فعلٌ دستوريٌّ حصريٌّ، أو فاعلٌ غيرُ مُشتَقّ، أو مصدرُ حقيقةٍ مزدوج، أو بوابةٌ بشريّة — فموضعُه سجلُّ القرارات.

---

## 4. قواعدُ التنفيذِ المُلزِمة

1. **قياسٌ قبل كلِّ خطوة**: حكمُ البوّابةِ يُسأَلُ ولا يُفترَض؛ وعددُ الدَّينِ يُعادُ توليدُه بالأداة.
2. **لا نمطَ يُنسَخُ آليًّا**: نمطُ 2A نموذجُ إثباتٍ، وكلُّ عمليّةٍ تُفحَصُ على حدَتِها (الأسئلةُ الخمسةَ عشرَ في § 9).
3. **لا كتابةَ بجانبِ الحدِّ تبقى مفتوحةً بعد الهجرة**: المسارُ القديمُ يُغلَقُ بـ`UndeclaredExecutionError` لا يُحذَفُ صامتًا.
4. **المعوّضُ حقيقيٌّ أو لا هجرة**: `pass` ليس معوّضًا.
5. **الفحوصُ القائمةُ تبقى قبلَ الحدّ**: التخويلُ المحلّيُّ طبقةٌ أولى لا يُستبدَل.
6. **لا معامَلَ تجاوزٍ** (`force`, `bypass`, `skip_check`, `unchecked`, `override`, `no_verify`, `unsafe`).
7. **لا تغييرَ لفعلٍ دستوريٍّ حصريٍّ إلى محيَّدٍ** لتجاوزِ `DENY`.
8. **عند العائقِ الدستوريِّ**: `HUMAN_DECISION_REQUIRED` — وتُعزَلُ المرحلةُ ويُواصَلُ ما لا يعتمدُ عليها. عائقٌ واحدٌ لا يوقفُ البرنامج.
9. **لا يُقالُ «تمّ» بلا دليل**: كلُّ مرحلةٍ تُرفَقُ بأرقامِ اختباراتِها وأرقامِ الجردِ قبلَها وبعدَها.

## 5. قواعدُ الالتزامِ والدفع

- التزامٌ **مستقلٌّ لكلِّ مرحلة**، برسالةٍ صريحةٍ بصيغةِ `sovereignty(<phase>): …`.
- لا يُدفَعُ التزامٌ يحملُ: كودَ تنقيح · مخلَّفاتِ تشغيل · أسرارًا · ملفّاتٍ مولَّدةً غيرَ مقصودة.
- قبلَ كلِّ دفع: `git status` · `git diff` · تشغيلُ الاختباراتِ المناسبة · فحصُ الأسرارِ والملفّاتِ الجديدة.
- الدفعُ يجري عبرَ Git Data API باعتمادٍ مُحقَونٍ في مستوى الشبكة، **ولا يُطبَعُ التوكنُ ولا يُكتَبُ في ملفٍّ ولا في رسالةِ التزام**، ثم يُوائَمُ `HEAD` المحلّيُّ بـ`fetch` + `reset --mixed` (لا `--hard`).
- بعدَ كلِّ دفعٍ يُسجَّلُ `sha` في § 8.

## 5.1 GOVERNING RULE — DOCUMENTATION BEFORE PUSH

**قاعدةٌ حاكمةٌ للمشروعِ كلِّه، لا توصيةً ولا أسلوبَ توثيق.** تنطبقُ على الوكلاءِ
والمطوّرينَ والمراجعينَ وكلِّ عمليّةٍ آليّةٍ تُعدِّلُ المستودعَ، وعلى كلِّ مرحلةٍ وقسمٍ
من برنامجِ الهجرةِ — لا على مرحلةٍ بعينِها، ولا تنتهي بانتهاءِ مرحلة.

### الترتيبُ الإلزاميّ
```
IMPLEMENT → VERIFY → DOCUMENT → COMMIT → PUSH
```
ويُمنَعُ: `IMPLEMENT → COMMIT → PUSH → DOCUMENT`، ويُمنَعُ
`IMPLEMENT → PUSH → DOCUMENT`. التوثيقُ **جزءٌ من التنفيذِ** لا وثيقةٌ ختاميّةٌ
تُكتَبُ بعدَ انتهاءِ العملِ كلِّه: لكلِّ قسمٍ
`WORK → VERIFY → UPDATE STATE → COMMIT → PUSH`.

### ما يجبُ أن يكونَ صحيحًا قبلَ كلِّ دفع
| المحور | الشرط |
|---|---|
| **Git** | الفرعُ صحيحٌ · الأساسُ معروفٌ · `diff` و`status` مُراجَعانِ · لا مخلَّفاتٍ ولا أسرارٍ ولا تغييراتٍ غيرِ مرتبطة |
| **الاختبارات** | اختباراتُ القسمِ · الانحدارُ المناسبُ · اختباراتُ السيادةِ ذاتُ الصلةِ · أيُّ تدقيقٍ ساكنٍ مطلوب |
| **الوثائق** | حالةُ القسمِ · الحالةُ العامّةُ في § 8.0 · الدَّينُ · سجلُّ القرارات · التسليمُ عندَ الحاجة |

إن لم تتطابقِ الثلاثةُ: **لا دفع**.

### المستودعُ والوثيقةُ يصفانِ الحالةَ نفسَها
كودٌ يقولُ `MIGRATED` ووثيقةٌ تقولُ `PLANNED` حالةٌ **غيرُ صالحة**. ورقمُ دَينٍ
تغيَّرَ يُحدَّثُ في **كلِّ** وثيقةٍ رسميّةٍ تعتمدُ عليه — ولا يُقبَلُ رقمانِ مختلفانِ
إلّا إذا وصفتِ الوثيقتانِ خطَّ أساسٍ مختلفًا **مذكورًا صريحًا**. وسؤالٌ ظهرَ في
التنفيذِ (مثلُ Q-16) لا يجوزُ أن يغيبَ عن سجلِّ القرارات.

### القسمُ وحدةُ دفع
كلُّ قسمٍ يُنتِجُ تعديلَ كودٍ أو اختبارًا أو عقدًا أو وثيقةً أو إغلاقَ مسارٍ قديمٍ
هو **وحدةٌ تنفيذيّةٌ قابلةٌ للإقفال**، تُوثَّقُ وتُلتزَمُ وتُدفَعُ ثمّ يُنتقَلُ إلى
غيرِها. ولا يُؤجَّلُ دفعُ قسمٍ مكتملٍ مُختبَرٍ مستقلٍّ انتظارًا لقسمٍ لاحق. ولا يُجمَعُ
قسمانِ مستقلّانِ لتقليلِ عددِ الالتزامات. وإذا كانَ قسمانِ مرتبطينِ بنيويًّا بحيثُ لا
يصحُّ أحدُهما منفردًا فهما وحدةٌ واحدةٌ **ويُوثَّقُ سببُ الجمع**. ولا حدَّ أدنى لحجمِ
التغييرِ يُعفي من الدفع، ولا حدَّ أعلى يُبيحُ التجميعَ بلا سبب.

### لا التزامَ يُلبِسُ مشكلةً لباسَ نجاح
قسمٌ فشلَ لا يُسجَّلُ `MIGRATED`، ولا يُدفَعُ التزامٌ يُوهِمُ نجاحَه، ولا يُوضَعُ له
حلٌّ يخالفُ العقد. يُصنَّفُ صريحًا (`BLOCKED` · `HUMAN_DECISION_REQUIRED` ·
`DEFERRED` · `TECHNICAL_FIXABLE`) مع السببِ والأدلّةِ وما أُنجِزَ قبلَ الفشلِ وما
يمنعُ الإقفال. وما صحَّ دمجُه مستقلًّا رغمَ العائقِ يُدمَجُ **مع توثيقِ أنَّه ليس
إغلاقًا للمرحلةِ المحجوبة**.

### رسالةُ الالتزامِ تُقرأُ بلا ذاكرة
تُمنَعُ الرسائلُ الغامضةُ (`update` · `fix` · `changes`). الرسالةُ تصفُ ما حدثَ فعلًا:
`sovereignty(P5): migrate open_case` · `governance(P6): establish treasury source-of-truth analysis`.

### لا فقدانَ سياقٍ بينَ الوكلاء
مَن أنهى قسمًا يتركُ خلفَه: كودًا يعملُ · اختباراتٍ ناجحةً · وثائقَ محدَّثةً · حالةَ
مشروعٍ محدَّثةً · التزامًا ودفعًا معروفَينِ · قائمةَ عوائقَ · خطوةً تاليةً صريحة.
**المستودعُ + الوثائقُ = مصدرُ الحالةِ التشغيليّ**، لا وصفٌ شفهيٌّ ولا ذاكرةُ محادثة.

### حفظُ الحقيقة
لا يُستخدَمُ التوثيقُ لتجميلِ الحالة. يُقالُ الفشلُ والانحدارُ والعائقُ والقرارُ
المطلوبُ وارتفاعُ الدَّينِ المؤقّتُ والمرحلةُ غيرُ المكتملةِ والاختباراتُ الساقطةُ كما
هي. **الحقيقةُ التشغيليّةُ أهمُّ من صورةِ المشروع.**

### مركزيّةُ القاعدة
هذه الوثيقةُ موضعُ القاعدةِ الوحيد؛ لا تُنشَأُ لها وثيقةٌ منافسة. وتُورَّثُ في كلِّ
`STAGE_*_HANDOFF.md` لاحقٍ بالإشارةِ إلى § 5.1 هنا، فيرثُها كلُّ وكيلٍ جديد.

## 6. معجمُ الحالات

| الحالة | المعنى |
|---|---|
| `PROVEN` | مُهاجَرةٌ ومُثبَتةٌ بالاختبارِ والقياس، ومسارُها القديمُ مُغلَق |
| `MIGRATED` | نُقِلَت وأُغلِقَ بديلُها، وينتظرُ اكتمالَ الإثبات |
| `PARTIAL` | جزءٌ من الخدمةِ فقط |
| `BLOCKED` | اعتماديّةٌ تقنيّةٌ تمنعُ التنفيذَ الآن |
| `HUMAN_DECISION_REQUIRED` | قرارٌ سياديٌّ/بشريٌّ لا يجوزُ للمُنفِّذِ اختراعُه |
| `ANALYZED` | حُلِّلَت ولم تُغيَّر |
| `DEFERRED` | مؤجَّلةٌ عمدًا بسببٍ مُعلَن |

## 7. خريطةُ المراحل

| المرحلة | النطاق | الحالة |
|---|---|---|
| **P0** | خطُّ الأساسِ وإدارةُ البرنامجِ وأداةُ الجردِ وسجلُّ القرارات | ✅ منجَزة |
| **P1** | إكمالُ عائلةِ `state_registry` (حالةُ المؤسسة · الإدارات · المسؤولون) | ✅ منجَزة · إغلاقُ `main.py` مؤجَّلٌ إلى P13 |
| **P2** | إعادةُ الجردِ والقياسِ + `STAGE_2B_HANDOFF.md` | ✅ منجَزة |
| **P3** | `state_runtime` القابلُ للهجرةِ بلا قرارٍ جديد | **BLOCKED** · Q-13 · Q-14 |
| **P4** | `agent_identity` / `population` — ما لا يمسُّ بوابةً بشريّة | **BLOCKED** · Q-15 |
| **P5** | *(انحرافٌ مُعلَنٌ — انظرْ أدناه)* عائلةُ `government_services` | ✅ PARTIAL/BLOCKED |
| **P6** | تحليلُ الخزانةِ (`TREASURY_MIGRATION_ANALYSIS.md`) بلا هجرةِ مال | قيدَ التنفيذ |
| **P7** | تحليلُ القضاءِ والتشريعِ والموافقاتِ والترقية | معلَّقة |
| **P8** | سياسةُ التعشيشِ (`SOVEREIGN_NESTING_POLICY.md`) — تحليلٌ لا اختراع | معلَّقة |
| **P9** | الاقتصادُ المركَّب | معلَّقة (تعتمدُ على P8) |
| **P10** | حسمُ مصدرِ الحقيقةِ الملكيِّ للمؤسسات | معلَّقة |
| **P11** | حياةُ النظامِ / النسخُ والاستعادة | معلَّقة |
| **P12** | جردٌ شاملٌ مُعادٌ | معلَّقة |
| **P13** | إغلاقُ ما تبقّى ممكنًا من الدَّين | معلَّقة |
| **P14** | التدقيقُ السياديُّ النهائيُّ (`SOVEREIGN_MIGRATION_FINAL_AUDIT.md`) | معلَّقة |

الترتيبُ مبدئيٌّ: يُعدَّلُ إذا أثبتَ رسمُ الاعتماديّاتِ سببًا أقوى، **مع توثيقِ السبب**.

> **انحرافُ ترقيمٍ مُعلَنٌ (لا يُصحَّحُ بصمتٍ في تاريخِ الالتزامات):** الخريطةُ الأصليّةُ
> جعلتْ P5 تحليلَ الخزانة، لكنَّ التنفيذَ الفعليَّ صرفَ P5 إلى عائلةِ
> `government_services` — وهي هجرةٌ لا تحتاجُ قرارًا بشريًّا جديدًا، فكانتْ أَولى من
> تحليلٍ يتوقّفُ على Q-5. وقد التزمَ التاريخُ بالأسماءِ `P5أ…P5ز` ودُفِعَ بها، فلا
> يُعادُ ترقيمُها بأثرٍ رجعيّ: تاريخُ الالتزاماتِ حقيقةٌ لا تُجمَّل. والخزانةُ صارتْ
> **P6**، والقضاءُ والتشريعُ ضُمَّا في **P7**، وما بعدَهما على ترقيمِه.


---

## 8.0 سجلُّ الحالةِ التشغيليِّ المركزيّ — يُحدَّثُ قبلَ كلِّ دفع

**آخرُ تحديثٍ:** 2026-08-18 · **الفرع:** `main` · **آخرُ التزامٍ مدفوعٍ:** `15ad9b7`
(اعتمادُ القاعدةِ الحاكمةِ وهذا السجلّ · سابقُه `a53a5fd`) · **الدَّينُ الحاليُّ
(مقياسُ v3):** **171**

| المرحلة | الحالة | ما تمّ | ما بقي | الدَّينُ بعدَها | الالتزام |
|---|---|---|---|---|---|
| P0 | ✅ منجَزة | خطُّ أساسٍ مقيسٌ · أداةُ جردٍ دائمة · سجلُّ قرارات | — | 185 (v1) | `7823bf1` |
| P1 | ✅ منجَزة | 4 كتاباتٍ في `state_registry` مُهاجَرة | إغلاقُ مسارِ `main.py` مؤجَّلٌ (P13) | 177 (v3) | `e814cd2` · `b7a3691` · `a00f12e` |
| P2 | ✅ منجَزة | إعادةُ الجردِ وخِتامُ 2B | — | 177 | `bcdeccc` |
| P3 | **BLOCKED** | تحليلُ `state_runtime` كاملًا | Q-13 · Q-14 قرارٌ بشريّ | 177 | `8e93684` |
| P4 | **BLOCKED** | تحليلُ هويّةِ الوكيلِ والسكّان | Q-15 قرارٌ بشريّ | 177 | `d0ce623` |
| P5 | **PARTIAL / BLOCKED** | 6 كتاباتٍ في الخدماتِ الحكوميّةِ مُهاجَرةٌ (P5أ–P5و) | `process_case` محجوبةٌ — Q-16 | **171** | `df850ea` · `1e9f817` · `64b32ff` · `a53a5fd` |
| **P6** | **IN_PROGRESS** | § 5.1 و§ 8.0 · نطاقٌ في § 8.2 · **P6-A و P6-B** مُنجَزانِ تحليلًا في `TREASURY_MIGRATION_ANALYSIS.md` (Q-5 محسومٌ جزئيًّا بالدليل · تفرَّعَ Q-17…Q-20) | P6-C تصنيفُ العمليّات — مكتوبٌ في § 6 من وثيقةِ الخزانة وينتظرُ الدفعَ | **171** (لم يتغيّرْ — لا هجرةَ مال) | `15ad9b7` · `82ebc7c` · P6-A+B: يُسجَّلُ عند الدفع |
| P7…P14 | معلَّقة | — | كما في § 7 | — | — |

**آخرُ مرحلةٍ مكتملةٍ:** P5 في وضعِها النهائيِّ (6 مُهاجَرةٌ + واحدةٌ محجوبة).
**المرحلةُ قيدَ التنفيذ:** P6 — القسمانِ A و B مُنجَزانِ ومدفوعان؛ P6-C هو الباقي.
**المرحلةُ التالية:** P7 — القضاءُ والتشريع (Q-6 · Q-7).

**مُخرَجُ P6 بلا تلطيف:** 32 كتابةَ مالٍ عامّةً في ثلاثةِ مواضعَ لا تعبرُ الحدَّ، و**لا واحدةَ منها قابلةٌ للهجرةِ اليومَ بلا قرارٍ بشريّ**. العائقُ دستوريٌّ (تسميةُ الفعلِ وفاعلُه) لا هندسيّ. والنمطُ الشرعيُّ مُثبَتٌ في 2A لكتابةِ ميزانيّةٍ واحدةٍ، ولم يُنسَخْ إلى خزانةِ الدولةِ لأنَّ تعميمَه نقلُ اختصاصٍ بينَ فرعين.

### العوائقُ القائمةُ الآن (كلُّها في `SOVEREIGN_DECISION_REGISTER.md`)
| السؤال | الموضع | ما يمنعُ الإقفال |
|---|---|---|
| Q-2 · Q-3 · Q-4 | تسميةُ الأفعالِ · اشتقاقُ الفرعِ · مصدرُ حقيقةِ المؤسسات | قرارٌ بشريّ |
| Q-8 · Q-11 · Q-12 | البوابةُ البشريّةُ R-001-1 · هويّةُ الإذنِ بدقّةِ الثانيةِ · إعادةُ عمليّةٍ فاشلة | قرارٌ بشريّ |
| Q-13 · Q-14 | `register_state` يؤدّي فعلًا ملكيًّا · `suspend_state` بلا حَكَم | قرارٌ بشريّ (P3) |
| Q-15 | تسميةُ فعلِ إنشاءِ الوكيلِ تُحدِّدُ تغطيةَ مفتاحِ القتل | قرارٌ بشريّ (P4) |
| **Q-16** | `process_case`: تشغيلُ المهمّةِ ليس أثرًا يُعكَس | قرارٌ بشريّ (P5ز) |
| **Q-5** | مصدرُ حقيقةِ المال | **محسومٌ جزئيًّا بالقياس (P6):** `state_treasury` أصلٌ و`national_economy` مشتقٌّ — بدليلِ اتّجاهِ المفاتيحِ الأجنبيّةِ وقيدِ `RESTRICT` |
| **Q-17** | هل `amos-credit` في `governance/treasury.py` مالٌ دستوريًّا؟ (9 كتاباتٍ كلُّها ALLOW لأيِّ فاعلٍ اليوم) | قرارٌ بشريّ (P6) |
| **Q-18** | إسنادُ أفعالِ المالِ إلى معجمِ الدستورِ أو توسيعُه — التسميةُ النطاقيّةُ تمرُّ `ALLOW` والاسمُ الدستوريُّ `DENY` بـ`R-003-1` | قرارٌ بشريّ (P6) |
| **Q-19** | من يحملُ فاعلَ الخزانةِ داخلَ خدمةٍ تنفيذيّة؟ | قرارٌ بشريّ (P6) |
| **Q-20** | ثلاثةُ تمثيلاتٍ لمبلغٍ واحد: `Numeric(20,4)` · `Float` · `String` | قرارٌ بشريّ (P6 · يمسُّ بياناتٍ قائمة) |
| Q-6 · Q-7 · Q-9 · Q-10 | القضاءُ · التشريعُ · التعشيشُ · النسخُ | تحليلٌ لم يُنجَزْ بعد |

### القياساتُ الأخيرةُ — مأخوذةٌ من تشغيلٍ فعليٍّ لا من تقدير
| القياس | القيمة | كيفَ يُعادُ توليدُها |
|---|---|---|
| دَينُ الهجرة | **171** | `python tools/audit/sovereign_write_inventory.py` |
| انحدارُ الخدماتِ الفيدراليّة | **255 passed** | § 8.1 أدناه (الترتيبُ مقصود) |
| مجموعةُ السيادة | **795 passed · 1 skipped** | `PYTHONPATH=federal/executive/services/src python -m pytest tests/sovereignty -q` |
| برهانُ الخدماتِ الحكوميّةِ (2B) | **39 passed** | `tests/test_2b_government_services_sovereign.py` |
| كتاباتُ المالِ العامّةُ غيرُ المُهاجَرة | **32** (7 خزانةُ الدولةِ + 9 اقتصادُ الوكلاءِ + 16 الاقتصادُ الوطنيّ) | `sovereign_write_inventory.py --service <path>` |
| أحكامُ البوابةِ على أفعالِ المال | 20 فعلًا × 3 فاعلين | `python tools/audit/treasury_gate_probe.py` → `docs/audit/evidence/treasury_gate_matrix.json` |

### 8.1 ترتيبُ مجموعةِ الانحدارِ — ليسَ تفصيلًا شكليًّا
```
cd federal/executive/services && PYTHONPATH=src python -m pytest \
  tests/test_r7_state_registry.py tests/test_2a_sovereign_runtime_integration.py \
  tests/test_2b_state_registry_sovereign.py tests/test_2b_government_services_sovereign.py \
  tests/test_phase12_states.py tests/test_treasury.py tests/test_1n_boundary_integration.py \
  tests/test_r7_government_services.py tests/test_r8_federal_state_integration.py -q
```
الترتيبُ مقصودٌ: ثمّةَ ارتباطُ ترتيبِ استيرادٍ **سابقٌ للبرنامجِ** (مفاتيحُ أجنبيّةٌ
بينَ `state_authority_grants` و`state_budgets`/`state_accounts`)، فترتيبٌ آخرُ يُنتِجُ
أخطاءَ تهيئةٍ زائفةً لا علاقةَ لها بالتعديلات. الأثرُ مُوثَّقٌ في
`STAGE_2B_HANDOFF.md` ومؤجَّلٌ إلى P12.

### 8.2 نطاقُ P6 — تحليلُ الخزانةِ ومصدرُ الحقيقة · بدأَ 2026-08-18

**الحالة:** IN_PROGRESS · **المُخرَجُ المُلزِم:** `TREASURY_MIGRATION_ANALYSIS.md` ·
**الحدُّ الصريح:** لا هجرةَ مالٍ في هذه المرحلة.

#### ما يدخلُ في P6
1. جردُ كلِّ كتابةٍ مُغيِّرةٍ في `state_treasury` و`federal_treasury` وما يتّصلُ بهما،
   بأداةِ الجردِ نفسِها لا بقراءةٍ عينيّة.
2. الجوابُ المقيسُ على Q-5: **أيُّ خزانةٍ مصدرُ الحقيقةِ للمال؟** فإن كانَ للمالِ
   مصدرانِ فالهجرةُ قبلَ الحسمِ تُثبِّتُ الالتباسَ في عقدٍ سياديّ.
3. رسمُ الأفعالِ الدستوريّةِ التي تمسُّها كلُّ كتابةٍ ماليّةٍ، وأيُّها حصريٌّ لجهةٍ،
   وأيُّها محكومٌ ببوابةٍ بشريّةٍ أو مفتاحِ قتل.
4. تصنيفُ كلِّ عمليّةٍ: قابلةٌ للهجرةِ بلا قرارٍ جديد · محجوبةٌ بقرارٍ · مؤجَّلةٌ بسببٍ
   مُعلَن — **بلا تنفيذِ أيِّها**.
5. أثرُ ارتباطِ ترتيبِ الاستيرادِ المعروفِ (`state_authority_grants` ←
   `state_budgets`/`state_accounts`) على أيِّ هجرةٍ ماليّةٍ لاحقة.

#### ما لا يدخلُ في P6 (ويُمنَعُ منعًا صريحًا)
- أيُّ تعديلٍ إنتاجيٍّ في كتابةٍ ماليّة، وأيُّ إغلاقِ مسارٍ قديمٍ في الخزانة.
- اختراعُ مصدرِ حقيقةٍ للمال، أو ترجيحُ إحدى الخزانتينِ بلا دليلٍ من المستودع.
- تحويلُ الالتباسِ إلى «تفصيلٍ تقنيّ» يُحسَمُ بذوقِ المُنفِّذ.

#### أقسامُ P6 — كلٌّ منها وحدةُ دفعٍ عندَ إنتاجِه تغييرًا
| القسم | الموضوع | يُنتِجُ تغييرًا؟ |
|---|---|---|
| P6-A | جردُ كتاباتِ الخزانةِ مقيسًا | نعم — وثيقةٌ + مُخرَجُ أداة |
| P6-B | حسمُ مصدرِ الحقيقةِ (Q-5) بالدليلِ أو تقييدُه قرارًا بشريًّا | نعم — وثيقةٌ + سجلُّ قرارات |
| P6-C | تصنيفُ العمليّاتِ وترتيبُ هجرةٍ مقترحٍ لمرحلةٍ لاحقة | نعم — وثيقة |

**حالةُ الأقسام:** P6-A و P6-B **ANALYZED ومدفوعان** في وحدةٍ واحدةٍ لأنَّ الجردَ وحسمَ مصدرِ الحقيقةِ غيرُ قابلينِ للفصل: الحسمُ نفسُه نتيجةُ الجردِ (اتّجاهُ المفاتيحِ الأجنبيّةِ ومصفوفةُ البوابة)، وفصلُهما يُنتِجُ التزامًا يعرضُ قياسًا بلا استنتاجٍ ثمّ استنتاجًا بلا قياس. وهذا هو الاستثناءُ المنصوصُ في § 5.1 «إن كانَ القسمانِ غيرَ قابلينِ للفصلِ بنيويًّا فوحدةٌ واحدةٌ مع بيانِ السبب».

لا يُنتقَلُ إلى P7 قبلَ إقفالِ الثلاثةِ أو تقييدِ ما مُنِعَ منها وسببِه.

## 8. سجلُّ المراحلِ والالتزامات

| المرحلة | ما تمّ | الاختبارات | الدَّينُ قبل | الدَّينُ بعد | الالتزام |
|---|---|---|---|---|---|
| P0 | مواءمةُ الأساسِ بلا فقدِ تعديل · أداةُ جردٍ دائمة · سجلُّ قرارات · وثيقةُ برنامج · نقلُ جردِ الدَّينِ إلى المستودع | 163 passed (خطُّ أساسٍ أخضر) | 185 (v1) | 185 (لا تغييرَ إنتاجيّ) | `7823bf1` — مدفوعٌ إلى GitHub |
| P1أ | هجرةُ `state_registry.set_institution_status` عبرَ الحدِّ · معاونٌ واحدٌ يكتبُ الحالةَ ويُعوِّضُها · اكتشافُ عيبٍ في أداةِ القياسِ وإصلاحُه (v2) وإعادةُ قياسِ خطِّ الأساسِ به · اكتشافُ قيدِ هويّةِ الإذنِ (Q-11) وخللِ إعادةِ العمليّةِ الفاشلةِ (Q-12) وتصحيحُ ادّعاءٍ في اختبارِ G-7 من 2A | 18 اختبارًا جديدًا + 181 في مجموعةِ الانحدار + 795 في مجموعةِ السيادةِ (1 skipped) — كلُّها خضراء | 208 (v2) | **207** (v2) | `e814cd2` — مدفوعٌ إلى GitHub |

| P1ب | هجرةُ `state_registry.create_department` عبرَ الحدِّ · مُعوِّضٌ يحذفُ الصفَّ فعلًا · هدفٌ يُظهِرُ تبعيّةَ الإدارةِ لمؤسستِها · إثباتُ أنَّ الحرسَ الطبيعيَّ (منعُ تكرارِ الرمز) ما زالَ يسبقُ الحدَّ | 30 اختبارًا في ملفِّ الإثبات + 193 في الانحدار + 795 في السيادةِ (1 skipped) | 207 | **206** | `b7a3691` — مدفوعٌ إلى GitHub |
| P1ج | `appoint_official` | MIGRATED | 13 | 181 | **177** (بمقياسِ v3) | `a00f12e` — مدفوعٌ |
| P1د | `revoke_official` | MIGRATED | 10 | — | **177** (مع P1ج) | `a00f12e` — مدفوعٌ |
| P2 | إعادةُ الجردِ وخِتامُ 2B | ANALYZED | — | 177 | 177 | `bcdeccc` — مدفوعٌ |
| P3 | State Runtime | **BLOCKED** (Q-13 · Q-14) | — | 177 | 177 | `8e93684` — مدفوعٌ |
| P4 | هويّةُ الوكيلِ والسكّان | **BLOCKED** (Q-15) | — | 177 | 177 | `d0ce623` — مدفوعٌ |
| P5أ | `set_service_status` (خدماتٌ حكوميّة) | **MIGRATED** | 13 | 177 | **176** | `df850ea` — مدفوعٌ |
| P5ب | `publish_service` | **MIGRATED** | 21 (الملفُّ كلُّه) | 176 | **175** | `1e9f817` — مدفوعٌ |
| P5ج | `open_case` (أثرانِ مُعلَنان) | **MIGRATED** | 27 (الملفُّ كلُّه) | 175 | **174** | `64b32ff` — مدفوعٌ |
| P5د | `assign_case` | **MIGRATED** | 31 (الملفُّ كلُّه) | 174 | **173** | `a53a5fd` — مدفوعٌ |
| P5هـ | `close_case` | **MIGRATED** | 35 (الملفُّ كلُّه) | 173 | **172** | `a53a5fd` — مدفوعٌ |
| P5و | `decide_case` (أثرانِ مُعلَنان) | **MIGRATED** | 39 (الملفُّ كلُّه) | 172 | **171** | `a53a5fd` — مدفوعٌ · الانحدارُ 255 · السياديّةُ 795 |
| P5ز | `process_case` | **BLOCKED** · Q-16 | — | 171 | 171 | `a53a5fd` — مدفوعٌ |
| P6-A+B | جردُ المالِ مقيسًا · حسمُ Q-5 بالدليلِ · تقييدُ Q-17…Q-20 | **ANALYZED** (لا هجرةَ مال) | — (وثائقُ فقط · الانحدارُ لم يُمَسّ) | 171 | 171 | يُسجَّلُ عند الدفع |

---

## 9. الأسئلةُ الخمسةَ عشرَ — تُسأَلُ قبلَ كلِّ عمليّة

1. مَن يملكُ الفعل؟ 2. ما الفعلُ الدستوريُّ الحقيقيُّ إن وُجِد؟ 3. مَن الفاعلُ الحقيقيّ؟
4. من أينَ جاءَ الفاعل؟ 5. هل تُغيِّرُ العمليّةُ حالةً؟ 6. ما مصدرُ الحقيقة؟
7. هل يوجدُ مصدرُ حقيقةٍ ثانٍ؟ 8. هل للأثرِ تركيبٌ عابرٌ لخدمات؟ 9. هل التعويضُ حقيقيّ؟
10. هل الإعادةُ آمنة؟ 11. هل توجدُ بوابةٌ بشريّة؟ 12. هل يوجدُ مسارُ تجاوز؟
13. هل يوجدُ نداءٌ سياديٌّ متعشِّش؟ 14. هل تغييرُ اسمِ الفعلِ يغيّرُ التفويض؟
15. هل الهجرةُ نفسُها تغيّرُ دلالاتَ النظام؟
